import React from 'react';
import { shallow } from 'enzyme';
import Discussion from './Discussion.js';

it('renders without crashing', () => {
  const wrapper = shallow(<Discussion />);
  // TODO.
});
