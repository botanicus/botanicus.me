import React from 'react';
import ReactDOM from 'react-dom';
import TagList from './TagList.js';

it('renders without crashing', () => {
  // const div = document.createElement('div');
  // ReactDOM.render(<TagList />, div);
});
